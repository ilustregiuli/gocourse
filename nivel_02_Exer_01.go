package main

import (
	"fmt"
)

// fazer um programa que mostre um número no formato decimal, binário e hexadecimal

func main() {

	x := 200
	fmt.Printf("%d = decimal\n%b = binário\n%#x = hexadecimal", x,x,x)
	
}
